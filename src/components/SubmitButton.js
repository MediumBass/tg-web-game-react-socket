import React from 'react';

const SubmitButton = (props) => {
    console.log(props.dmg)
    return (
        <div>
            <button type="button">Use Skill</button>
        </div>
    );
};

export default SubmitButton;